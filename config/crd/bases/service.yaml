apiVersion: v1
kind: Service
metadata:
  name: karpenter-exoscale
  labels:
    app: karpenter-exoscale
spec:
  selector:
    app: karpenter-exoscale
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
  clusterIP: None
